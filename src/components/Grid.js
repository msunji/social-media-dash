import styled from "styled-components";

export const Grid = styled.main`
  display: grid;
  grid-template-columns: 1fr;
  width: 90%;
  margin-left: auto;
  margin-right: auto;
  padding-top: 3rem;
  position: relative;
`;
